package edu.cs.umass.Kmeans;

import cc.factorie.app.nlp._

/**
 * Hello world!
 *
 */
object KMeans {
  new Document("Blah")


  def main(args:Array[String]) {
    for (i <- 1 to 5) {
      println("I'm at %d".format(i))
    }

    (1 to 5).foreach{i => println("I'm at %d".format(i))}
  }
}


